[project]
name = "agent-tuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ettore", email = "ettore.caputo27@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    
]

[project.scripts]
agent-tuning = "agent_tuning:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
benchmark = [
    "dotenv>=0.9.9",
    "langchain-ollama>=1.1.0",
    "tqdm>=4.70.1",
]
unsloth-training = [
    "accelerate>=1.15.0",
    "bitsandbytes>=0.50.2",
    "datasets>=4.3.0",
    "dotenv>=0.9.9",
    "numpy>=2.5.3",
    "peft>=0.21.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "sentence-transformers>=6.1.0",
    "torch>=2.12.1",
    "tqdm>=4.70.1",
    "transformers>=5.5.0",
    "trl==0.24.0",
    "unsloth==2026.9.7",
    "unsloth-zoo==2026.9.6",
    "wandb>=0.30.0",
]
