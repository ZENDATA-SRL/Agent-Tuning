annotated-types==0.7.0
anyio==4.13.0
backoff==2.2.1
boto3==1.43.9
botocore==1.43.9
certifi==2026.4.22
charset-normalizer==3.4.7
distro==1.9.0
googleapis-common-protos==1.75.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.15
importlib_metadata==8.7.1
jiter==0.14.0
jmespath==1.1.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain==1.3.1
langchain-aws==1.4.7
langchain-core==1.4.0
langchain-ollama==1.1.0
langchain-openai==1.2.1
langchain-protocol==0.0.15
langfuse==4.6.1
langgraph==1.2.0
langgraph-checkpoint==4.1.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.3.14
langsmith==0.8.5
numpy<2.4
ollama==0.6.2
openai==2.37.0
opentelemetry-api==1.42.1
opentelemetry-exporter-otlp-proto-common==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1
opentelemetry-proto==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-semantic-conventions==0.63b1
orjson==3.11.9
ormsgpack==1.12.2
packaging==26.2
protobuf==6.33.6
pydantic==2.13.4
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
regex==2026.5.9
requests==2.34.2
requests-toolbelt==1.0.0
s3transfer==0.17.0
six==1.17.0
sniffio==1.3.1
tenacity==9.1.4
tiktoken==0.13.0
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uuid_utils==0.15.0
wrapt==1.17.3
xxhash==3.7.0
zipp==3.23.1
zstandard==0.25.0

# ── Training (SFT via Unsloth + QLoRA) ────────────────────────────────────
# Pins kept loose because Unsloth itself imposes a precise (and frequently
# updated) compatibility matrix on transformers/trl/peft/accelerate. Letting
# pip resolve within Unsloth's constraints avoids conflicts when bumping.
#
# Minimum versions are dictated by Gemma-4 support:
#   - transformers>=5.5.2  → Gemma-4 added in 5.5.0, 5.5.2 fixes use_cache=False
#                            path which Unsloth's training loop relies on.
#   - unsloth>=2026.4.2    → first day-0 release with Gemma-4 (E2B/E4B/26B/31B)
#                            and the gradient-accumulation / use_cache fixes.
unsloth>=2026.5.6
unsloth_zoo
# transformers 5.5.0 is excluded by vLLM (!=5.5.0); 5.5.2 is the lowest version
# accepted by both vLLM and required for Gemma-4 support in unsloth.
# NOTE: unsloth_zoo metadata incorrectly declares <=5.5.0 — this is a known
# upstream packaging bug; the code runs correctly on 5.5.2.
# NOTE: llmcompressor pulls transformers<=4.57.6; always re-pin after installing:
#   pip install "transformers==5.5.2"
transformers==5.5.2
# W4A16 quantisation for vLLM export — official AutoAWQ successor by vLLM project.
# AutoAWQ is deprecated and does not support Gemma4.
llmcompressor
trl
peft
accelerate
bitsandbytes
datasets
sentencepiece
xformers
# Experiment tracking — used by train/wandb_setup.py when --wandb (or any
# WANDB_* env var) is set. Pin loose: wandb is generally backwards
# compatible across minor versions and HF/TRL track its API changes.
wandb
# torch/triton are CUDA-specific; let Unsloth's installer select the right
# wheel for the local CUDA toolkit (see https://docs.unsloth.ai for the
# platform-specific install command).
